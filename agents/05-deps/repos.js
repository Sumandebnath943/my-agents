// agents/05-deps/repos.js
export const OWNER = "Sumandebnath943";

// Repos are discovered LIVE from the GitHub API each run; only those with a
// package.json are scanned (non-Node repos are skipped automatically).
// Use IGNORE to exclude any repo by name.
export const IGNORE = [];
