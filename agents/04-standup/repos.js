// agents/04-standup/repos.js
export const OWNER = "Sumandebnath943";

// Repos are discovered LIVE from the GitHub API each run (so new repos are picked up
// automatically). Use IGNORE to exclude any you never want in the standup
// (e.g. forks, throwaway experiments). Add bare repo names.
export const IGNORE = [];
